# results/

One auto-numbered folder per run: `test_NNN_<model>_<task>/`.

The number comes from scanning this folder for the highest one and adding one, so
two runs never collide and chronological order is readable from the listing alone.
Gaps are never reused.

See the top-level README for the full list of files each run produces. Nothing
here is version controlled.
